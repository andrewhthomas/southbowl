---
import '../styles/global.css';
import { league, navItems } from '../data/league';

interface Props {
  title?: string;
  activeNav?: string;
}

const { title = league.name, activeNav = '/' } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <title>{title} | {league.center}</title>
    <!-- Prevent flash: apply theme before paint -->
    <script is:inline>
      (function() {
        const theme = localStorage.getItem('theme') || 'dark';
        if (theme === 'light') document.documentElement.classList.add('light');
        var fs = localStorage.getItem('fontSize');
        if (fs) document.documentElement.style.fontSize = fs + 'px';
      })();
    </script>
  </head>
  <body class="min-h-screen bg-background text-foreground">
    <!-- Mobile header -->
    <header class="lg:hidden border-b border-border bg-card px-4 py-3">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-lg font-bold text-primary">{league.center}</h1>
          <p class="text-xs text-muted-foreground">{league.name}</p>
        </div>
        <div class="flex items-center gap-1">
          <button id="font-down-mobile" class="p-1.5 rounded-md hover:bg-secondary text-muted-foreground hover:text-foreground text-xs font-bold" aria-label="Decrease text size">A-</button>
          <button id="font-up-mobile" class="p-1.5 rounded-md hover:bg-secondary text-muted-foreground hover:text-foreground text-xs font-bold" aria-label="Increase text size">A+</button>
          <button id="theme-toggle-mobile" class="p-2 rounded-md hover:bg-secondary text-muted-foreground hover:text-foreground transition-colors" aria-label="Toggle theme">
            <!-- Sun icon (shown in dark mode) -->
            <svg class="w-5 h-5 theme-icon-sun" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
            </svg>
            <!-- Moon icon (shown in light mode) -->
            <svg class="w-5 h-5 theme-icon-moon hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
            </svg>
          </button>
          <button id="mobile-menu-btn" class="relative p-2 rounded-md hover:bg-secondary w-10 h-10" aria-label="Toggle menu">
            <svg class="w-6 h-6 menu-icon-open absolute inset-2 transition-all duration-300 ease-in-out opacity-100 rotate-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg class="w-6 h-6 menu-icon-close absolute inset-2 transition-all duration-300 ease-in-out opacity-0 rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
      <nav id="mobile-menu" class="grid grid-rows-[0fr] transition-[grid-template-rows] duration-300 ease-in-out mt-3 pt-3 border-t border-border">
        <ul class="space-y-1 overflow-hidden">
          {navItems.map(item => (
            <li>
              <a
                href={item.href}
                class:list={[
                  'block px-3 py-2 rounded-md text-sm font-medium transition-colors',
                  activeNav === item.href
                    ? 'bg-primary text-primary-foreground'
                    : 'text-muted-foreground hover:bg-secondary hover:text-foreground',
                ]}
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </header>

    <div class="flex">
      <!-- Desktop sidebar -->
      <aside class="hidden lg:flex lg:flex-col lg:w-64 lg:min-h-screen border-r border-border bg-card">
        <div class="p-6 border-b border-border">
          <div class="flex items-center justify-between">
            <h1 class="text-xl font-bold text-primary">{league.center}</h1>
            <button id="theme-toggle-desktop" class="p-1.5 rounded-md hover:bg-secondary text-muted-foreground hover:text-foreground transition-colors" aria-label="Toggle theme">
              <svg class="w-4 h-4 theme-icon-sun" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg class="w-4 h-4 theme-icon-moon hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>
          </div>
          <p class="text-sm text-muted-foreground mt-1">{league.name}</p>
          <div class="mt-3 space-y-1 text-xs text-muted-foreground">
            <p>{league.address}</p>
            <p>{league.phone}</p>
            <p>{league.day}s at {league.time}</p>
            <p>Updated: {league.lastUpdated}</p>
          </div>
        </div>
        <nav class="flex-1 p-4">
          <ul class="space-y-1">
            {navItems.map(item => (
              <li>
                <a
                  href={item.href}
                  class:list={[
                    'block px-3 py-2.5 rounded-md text-sm font-medium transition-colors',
                    activeNav === item.href
                      ? 'bg-primary text-primary-foreground'
                      : 'text-muted-foreground hover:bg-secondary hover:text-foreground',
                  ]}
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
        <div class="p-4 border-t border-border text-xs text-muted-foreground space-y-3">
          <div class="flex items-center justify-between">
            <span>Text Size</span>
            <div class="flex items-center gap-1">
              <button id="font-down-desktop" class="w-7 h-7 rounded hover:bg-secondary flex items-center justify-center text-base font-bold" aria-label="Decrease text size">A-</button>
              <button id="font-reset-desktop" class="w-7 h-7 rounded hover:bg-secondary flex items-center justify-center text-xs" aria-label="Reset text size">Aa</button>
              <button id="font-up-desktop" class="w-7 h-7 rounded hover:bg-secondary flex items-center justify-center text-base font-bold" aria-label="Increase text size">A+</button>
            </div>
          </div>
          <p>League #{league.leagueId}</p>
          <p>Season starts {league.startDate}</p>
        </div>
      </aside>

      <!-- Main content -->
      <main class="flex-1 min-h-screen">
        <div class="p-4 lg:p-8 max-w-7xl">
          <slot />
        </div>
      </main>
    </div>

    <script>
      function updateIcons() {
        const isLight = document.documentElement.classList.contains('light');
        document.querySelectorAll('.theme-icon-sun').forEach(el => {
          (el as HTMLElement).style.display = isLight ? 'none' : 'block';
        });
        document.querySelectorAll('.theme-icon-moon').forEach(el => {
          (el as HTMLElement).style.display = isLight ? 'block' : 'none';
        });
      }

      function toggleTheme() {
        const isLight = document.documentElement.classList.toggle('light');
        localStorage.setItem('theme', isLight ? 'light' : 'dark');
        updateIcons();
      }

      document.getElementById('theme-toggle-desktop')?.addEventListener('click', toggleTheme);
      document.getElementById('theme-toggle-mobile')?.addEventListener('click', toggleTheme);

      const btn = document.getElementById('mobile-menu-btn');
      const menu = document.getElementById('mobile-menu');
      const iconOpen = btn?.querySelector('.menu-icon-open') as HTMLElement | null;
      const iconClose = btn?.querySelector('.menu-icon-close') as HTMLElement | null;
      let menuOpen = false;
      btn?.addEventListener('click', () => {
        menuOpen = !menuOpen;
        if (menu) {
          menu.style.gridTemplateRows = menuOpen ? '1fr' : '0fr';
        }
        if (iconOpen) {
          iconOpen.style.opacity = menuOpen ? '0' : '1';
          iconOpen.style.transform = menuOpen ? 'rotate(-90deg)' : 'rotate(0deg)';
        }
        if (iconClose) {
          iconClose.style.opacity = menuOpen ? '1' : '0';
          iconClose.style.transform = menuOpen ? 'rotate(0deg)' : 'rotate(90deg)';
        }
      });

      // Set initial icon state
      updateIcons();

      // Font size control
      const FONT_SIZES = [14, 15, 16, 17, 18, 20];
      const DEFAULT_SIZE = 16;
      let currentSize = parseInt(localStorage.getItem('fontSize') || String(DEFAULT_SIZE));
      document.documentElement.style.fontSize = currentSize + 'px';

      function setFontSize(size: number) {
        currentSize = Math.max(FONT_SIZES[0], Math.min(FONT_SIZES[FONT_SIZES.length - 1], size));
        document.documentElement.style.fontSize = currentSize + 'px';
        localStorage.setItem('fontSize', String(currentSize));
      }

      function stepFont(dir: number) {
        const idx = FONT_SIZES.indexOf(currentSize);
        const next = idx === -1 ? FONT_SIZES.indexOf(DEFAULT_SIZE) + dir : idx + dir;
        if (next >= 0 && next < FONT_SIZES.length) setFontSize(FONT_SIZES[next]);
      }

      document.getElementById('font-up-desktop')?.addEventListener('click', () => stepFont(1));
      document.getElementById('font-down-desktop')?.addEventListener('click', () => stepFont(-1));
      document.getElementById('font-reset-desktop')?.addEventListener('click', () => setFontSize(DEFAULT_SIZE));
      document.getElementById('font-up-mobile')?.addEventListener('click', () => stepFont(1));
      document.getElementById('font-down-mobile')?.addEventListener('click', () => stepFont(-1));
    </script>
  </body>
</html>
